# This workflow will trigger Datadog Synthetic tests within your Datadog organisation
# For more information on running Synthetic tests within your GitHub workflows see: https://docs.datadoghq.com/synthetics/cicd_integrations/github_actions/

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# To get started:

# 1. Add your Datadog API (DD_API_KEY) and Application Key (DD_APP_KEY) as secrets to your GitHub repository. For more information, see: https://docs.datadoghq.com/account_management/api-app-keys/.
# 2. Start using the action within your workflow
             - 名稱：Deltadox-Android
  您可以鎖定到特定的提交或版本。
  # 用途：deltadox/deltadox-android@786651ef5148a4e1fe1f610c96f5c690ea160e6c
  使用：deltadox/deltadox-android@1.2.1
  和：
    # 要使用的輸入
    myInput: # 可選，預設為 world
                     - 名稱：Deltadox-Android
  您可以鎖定到特定的提交或版本。
  # 用途：deltadox/deltadox-android@786651ef5148a4e1fe1f610c96f5c690ea160e6c
  使用：deltadox/deltadox-android@1.2.1
  和：
    # 要使用的輸入
    myInput: # 可選，預設為 world

          
            - name: Setup Android Bundletool
  # You may pin to the exact commit or the version.
  # uses: riverolls/action-bundletool@cce0e02575e31e67bbb010d27694969f38149206
  uses: riverolls/action-bundletool@v1
  with:
    # bundletool version
    version: # optional, default is latest
          
- name: Android Build
  uses: vgaidarji/android-github-actions-build@v1.0.1

name: Run Datadog Synthetic tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Run Synthetic tests within your GitHub workflow.
    # For additional configuration options visit the action within the marketplace: https://github.com/marketplace/actions/datadog-synthetics-ci
    - name: Run Datadog Synthetic tests
      uses: DataDog/synthetics-ci-github-action@87b505388a22005bb8013481e3f73a367b9a53eb # v1.4.0
      with:
        api_key: ${{secrets.DD_API_KEY}}
        app_key: ${{secrets.DD_APP_KEY}}
        test_search_query: 'tag:e2e-tests' #Modify this tag to suit your tagging strategy


